import './globals.css';

export const metadata = {
  title: 'Crystal Clinic Orthopedic — Furniture Selection',
  description: 'PR# 16658 Furniture Selection Configurator',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
